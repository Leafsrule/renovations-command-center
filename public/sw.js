// Placeholder service worker for the Phase 1 PWA-ready structure.
// Offline caching will be added after the first usable MVP is approved.
self.addEventListener("install", () => {
  self.skipWaiting();
});
